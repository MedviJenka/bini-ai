{
  "mcpServers": {
    "bini-vision": {
      "type": "http",
      "url": "http://localhost:6000/mcp"
    },
    "docker": {
      "type": "stdio",
      "command": "docker",
      "args": ["run", "-i", "--rm", "-v", "/var/run/docker.sock:/var/run/docker.sock", "mcp/docker"]
    }
  }
}
